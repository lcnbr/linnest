[package]
name = "linnest"
version = "0.2.0"
entrypoint = "src/lib.typ"
authors = [ "GammaLoop contributors" ]
license = "MIT"
description = "Typst wrapper package for the linnest WebAssembly graph layout plugin."
repository = "https://github.com/alphal00p/gammaloop"
keywords = [ "graph", "layout", "dot", "wasm" ]
categories = [ "visualization", "utility" ]
exclude = [ "docs", "examples" ]
disciplines = [ ]
